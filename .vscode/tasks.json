{
    "tasks": [
        {
            "type": "shell",
            "label": "buildbat",
            "command": "${workspaceFolder}/build.bat",
            "args": [
                "${workspaceFolder}",
                "User32.lib Ws2_32.lib"
            ],
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": [
                "$msCompile"
            ]
        }
    ],
    "version": "2.0.0"
}